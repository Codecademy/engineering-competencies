import { ICodeEventCallback } from './_interfaces';
export declare class CodeEvent<D = any, T = any> {
    protected _listeners: Map<T, ICodeEventCallback<D>[]>;
    get listeners(): Map<T, ICodeEventCallback<D>[]>;
    protected _name: string;
    constructor(name: string);
    dispatch(target: T, data: D): void;
    addEventListener(cb: ICodeEventCallback<D>, target?: T): void;
}
