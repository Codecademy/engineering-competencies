"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const createElement_1 = require("../createElement");
const _constants_1 = require("../_constants");
describe('SVG creation', () => {
    it('creates an SVG element with the right namespace', () => {
        const svg = createElement_1.createSVGElement({ id: 'svg' });
        expect(svg.tagName).toEqual("svg");
        expect(svg.namespaceURI).toEqual(_constants_1.SVG_NAMESPACE);
    });
    it('forces a namespace', () => {
        const svg = createElement_1.createSVGElement({ id: 'svg', namespace: 'kipprice.com' });
        expect(svg.namespaceURI).toEqual(_constants_1.SVG_NAMESPACE);
    });
    it('allows for nesting', () => {
        const shapes = {
            rect: null,
            circle: null
        };
        const svg = createElement_1.createSVGElement({
            id: 'svg',
            children: [
                { type: 'rect', key: 'rect' },
                { type: 'circle', key: 'circle' }
            ]
        }, shapes);
        expect(svg.namespaceURI).toEqual(_constants_1.SVG_NAMESPACE);
        expect(shapes.circle.tagName).toEqual('circle');
        expect(shapes.rect.tagName).toEqual('rect');
    });
});
