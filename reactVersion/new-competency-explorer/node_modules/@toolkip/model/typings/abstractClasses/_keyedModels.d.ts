import { _KeyedModel } from './_keyedModel';
import { IArrayModel } from '../_shared';
export declare abstract class _KeyedModels<T, K, X> extends _KeyedModel<T, K, X> implements IArrayModel<X, K, T> {
    protected _getType(): string;
    abstract add(value: X): boolean;
    remove(key: K): X;
    findIndex(val: X): K;
    contains(val: X): boolean;
    clear(): void;
}
