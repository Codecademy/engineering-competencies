import { _Model, _KeyedModels } from '../abstractClasses';
import { IArrayModel } from '../_shared/_interfaces';
export declare class MArray<T> extends _KeyedModels<T[], number, T> implements IArrayModel<T, number, T[]> {
    protected _getDefaultValues(): T[];
    protected _map(data: T[], mapFunc: (val: T, key: number) => void): void;
    protected _getValue(output: T[], key: number): _Model<T>;
    protected _setValue(output: T[], key: number, value: _Model<T>): void;
    protected _shouldSplice(output: T[], key: number, value: _Model<T>): boolean;
    add(element: T): boolean;
}
