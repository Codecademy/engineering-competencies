import { IConstructor } from '@toolkip/object-helpers';
import { IModel, IModelTransform } from '../_shared/_interfaces';
export declare const createModelTransform: <T>(ctor: IConstructor<IModel<T, any, any>>) => {
    incoming: (data: any) => T;
};
export declare const createModelDateTransform: (tx?: IModelTransform<string | Date>) => {
    incoming: (data: any) => Date;
};
