import { IDictionary } from '@toolkip/object-helpers';
import { IEqualityFunction } from '@toolkip/comparable';
import { IBindingDetails, BoundDeleteFunction as BoundSkipFunction, BoundUpdateFunction, BoundEvalFunction } from './_interfaces';
declare class _Binder {
    protected _boundDetails: IDictionary<IBindingDetails<any>>;
    protected _id: number;
    protected _started: boolean;
    protected _getNextId(): string;
    constructor();
    bind<T = any>(evalFunc: BoundEvalFunction<T>, updateFunc: BoundUpdateFunction<T>, skipFunc?: BoundSkipFunction, equalsFunc?: IEqualityFunction<T>): string;
    unbind(key: string): boolean;
    protected _startAnimationLoop(): void;
    protected _onFrame(): Promise<void>;
    protected _handlingBinding<T>(details: IBindingDetails<T>): Promise<void>;
}
export declare const Binder: _Binder;
export {};
