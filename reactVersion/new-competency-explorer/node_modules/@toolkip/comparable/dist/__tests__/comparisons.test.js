"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const comparisons_1 = require("../comparisons");
describe('comparisons', () => {
    describe('equals', () => {
        it('handles primitives', () => {
            expect(comparisons_1.equals('a', 'a')).toBeTruthy();
            expect(comparisons_1.equals(1, 2)).toBeFalsy();
            expect(comparisons_1.equals(undefined, undefined)).toBeTruthy();
        });
        it('handles equatables', () => {
            const objA = { equals: () => true };
            const objB = { name: 'A', equals: () => true };
            expect(comparisons_1.equals(objA, objB));
        });
        it('handles dates', () => {
            const dtA = new Date('2020-10-10');
            const dtB = new Date('2020-10-10');
            expect(comparisons_1.equals(dtA, dtB)).toBeTruthy();
        }),
            it('handles objects', () => {
                const objA = { name: 'Kip', age: 30 };
                const objB = { name: 'Kip', age: 30 };
                expect(comparisons_1.equals(objA, objB)).toBeTruthy();
            });
        it('handles arrays', () => {
            const arrA = [{ name: 'A' }, { name: 'b' }];
            const arrB = [{ name: 'A' }, { name: 'b' }];
            expect(comparisons_1.equals(arrA, arrB)).toBeTruthy();
        });
    });
    describe('lesser than', () => {
    });
});
