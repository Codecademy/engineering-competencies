import { Styles } from '@toolkip/style-helpers';
import { IDictionary } from '@toolkip/object-helpers';
export declare abstract class _Library {
    protected _rawStyles: IDictionary<Styles>;
    protected _elems: IDictionary<HTMLStyleElement>;
    protected abstract get _idSuffix(): string;
    protected _lastId: number;
    getNextId(): string;
    hasStyles(uniqueKey: string): boolean;
    add(uniqueKey: string, styles: Styles, force?: boolean): void;
    protected _shouldAdd(existingStyles: any, mergedStyles: any, force: any): boolean;
    remove(uniqueKey: string): boolean;
    private _merge;
    private _stringify;
    protected _updateElems(styles: Styles, uniqueKey?: string): void;
    protected _getOrCreateElem(uniqueKey: string): HTMLStyleElement;
    private _getOrCreateExistingStyles;
    getElemForKey(uniqueKey: string): HTMLStyleElement;
    get(uniqueKey: string): Styles;
}
