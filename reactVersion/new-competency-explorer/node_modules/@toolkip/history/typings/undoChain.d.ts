import { HistoryChain } from "./history";
import { Undoable } from "./_interfaces";
export declare abstract class _UndoChain extends HistoryChain<Undoable> {
    constructor();
    protected abstract _shouldIgnoreEvent(event: KeyboardEvent): boolean;
    undo(): void;
    redo(): void;
}
